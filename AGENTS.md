# Agent Instructions

在参与开发时，请严格遵守以下规则：

## 1. 核心设计原则
- **文档驱动**：总是先修改 `DESIGN.md` 再修改代码。`DESIGN.md` 是项目单点事实。
- **严禁表情**：任何情况（代码、注释、文档、Commit Message）下绝对不使用 emoji。
- **显式失败**：外部数据异常必须显式提示，不允许静默失败跳过。

## 2. 环境与依赖管理
- **容器优先**：使用 Docker 容器作为生产和调度运行入口。
- **包管理器**：严格使用 `uv` 管理 Python 环境和依赖。
- **唯一入口**：所有任务执行只允许从 `main.py` 单一入口启动。

## 3. 编码与开发规范
- **日志输出**：必须使用 `logging` 打印详细日志，绝对不要用 `print`。
- **类型注解**：Python 代码需包含完整的 Type Hints。
- **时间标准**：内部时间统一使用 UTC，展示时转换为北京时间或美东时间。
